# ReadQuest

Frontend: Vite + React + TypeScript + Tailwind CSS
Backend: Express + TypeScript + MongoDB/Mongoose

## Frontend

```bash
cd D:\readQuest\frontend
npm run dev
```

## Backend

Create `backend/.env` from `backend/.env.example`, then run:

```bash
cd D:\readQuest\backend
npm run dev
```
